#!/bin/bash
if [[ $(basename $(pwd)) == "bin" ]]; then
    cd ..
fi

# Parse flags
_yes=false
for _arg in "$@"; do
    case "$_arg" in
        -y|--yes) _yes=true ;;
        *) echo "Unknown option: $_arg"; exit 1 ;;
    esac
done
# Also treat CI environments as non-interactive
if [[ "${CI:-}" == "true" ]]; then
    _yes=true
fi

export QT_QPA_PLATFORM=xcb

if ! command -v juliaup &> /dev/null; then
    echo "Please install the Julia installer 'juliaup'!"
    echo "See: https://github.com/JuliaLang/juliaup"
    exit 1
fi

if ! command -v julia &> /dev/null; then
    echo "Julia is not installed. Which version do you want to install?"
    echo "  1) Julia 1.11"
    echo "  2) Julia 1.12"
    if [[ "$_yes" == "true" ]]; then
        _julia_choice=""
        echo "Non-interactive mode: using default (1.12)"
    else
        read -rp "Enter 1 or 2 [default: 2]: " _julia_choice
    fi
    case "${_julia_choice}" in
        1) _desired_julia="1.11" ;;
        2|"") _desired_julia="1.12" ;;
        *) echo "Invalid choice. Exiting."; exit 1 ;;
    esac
    echo "Installing Julia ${_desired_julia}..."
    juliaup add "${_desired_julia}"
    juliaup default "${_desired_julia}"
    echo "Julia ${_desired_julia} installed and set as default."
fi

julia_version=$(julia --version | awk '{print($3)}')
julia_major=${julia_version:0:3} 
if [[ $julia_major == "1.1" ]]; then
    julia_major=${julia_version:0:4} 
fi

# Verify supported Julia versions (1.11 or 1.12)
if [[ "$julia_major" != "1.11" && "$julia_major" != "1.12" ]]; then
    echo "Warning: Unsupported Julia version detected: $julia_version. This script supports Julia 1.11 or 1.12 only." >&2
    echo "Use the command 'juliaup default 1.11' or 'juliaup default 1.12' to switch to a supported version." >&2
    exit 1
fi

export KMP_DUPLICATE_LIB_OK=TRUE

PYTHON_PATH=$(which python3)
if [ -x "$PYTHON_PATH" ]; then
    echo "Python is found at $PYTHON_PATH"
    if $PYTHON_PATH -c "import matplotlib" &> /dev/null; then
        echo "Matplotlib found. Using existing installation."
        export PYTHON=$PYTHON_PATH
    else
        echo "Matplotlib is not found."
        if [[ "$_yes" == "true" ]]; then
            echo "Non-interactive mode: installing matplotlib with Conda."
            export PYTHON=""
        else
            read -p "Do you want to install matplotlib with Conda? (y/n): " choice
            case "$choice" in 
              y|Y ) 
                export PYTHON=""
                ;;
              n|N ) 
                echo "Exiting without installing matplotlib."
                exit 1
                ;;
              * ) 
                echo "Invalid choice. Exiting."
                exit 1
                ;;
            esac
        fi
    fi
else
    echo "Python is not found."
    exit 1
fi

# total memory in kB
MEM=$(grep MemTotal /proc/meminfo | awk '{print $2}')

if (( $MEM <  15000000 )); then
    MASKED=$(systemctl is-enabled systemd-oomd)
    if [[ $MASKED == "masked" ]]; then
        echo "Already masked!"
    fi
    export JULIA_IMAGE_THREADS=1
    export JULIA_PKG_PRESERVE_TIERED_INSTALLED=true
else
    export MPLBACKEND=qt5agg
fi


if test -f "kps-image-${julia_major}.so"; then
    mv bin/kps-image-${julia_major}.so bin/kps-image-${julia_major}.so.bak
fi

echo "Updating packages..."
if test -f "Manifest.toml"; then
   rm Manifest.toml
fi
julia --startup-file=no  --project -e "using Pkg; Pkg.add(\"PyCall\"); Pkg.build(\"PyCall\")"
if [[ $PYTHON == "" ]]; then
    julia --startup-file=no  --project -e "using Pkg; Pkg.add(\"Conda\"); using Conda; Conda.add(\"matplotlib\"); using ControlPlots"
fi
julia --startup-file=no  --project -e "using Pkg; Pkg.instantiate()"

julia  --project -t auto -e "using Pkg; Pkg.precompile()"
julia  --project -t auto --gcthreads=4,1 -e "include(\"./test/create_sys_image.jl\");"

mv kps-image_tmp.so bin/kps-image-${julia_major}.so
julia --project -e "using Pkg; Pkg.precompile(); using Parameters"
